Place your PDF files in this folder before running ingestion.

Quick start:
1. Copy one or more `.pdf` files into `docs/` (subfolders are supported).
2. Run `npm run ingest`.
3. Start the app with `npm run dev`.
